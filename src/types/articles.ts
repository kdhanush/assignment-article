export interface IArticle {
    id: string;
    title: string;
    summary: string;
  }

  export interface IArticleData {
    fullText: string;
    id: string;
    title: string;
    summary: string;
  }